[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn rapidtires.wsgi --log-file -"
healthcheckPath = "/api/service-request/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
